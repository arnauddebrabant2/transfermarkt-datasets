name: Weekly Transfermarkt Scrape

on:
  schedule:
    - cron: '0 4 * * 1'  # Elke maandag om 04:00 UTC
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install transfermarkt-scraper
        run: |
          pip install poetry
          git clone https://github.com/dcaribou/transfermarkt-scraper.git /tmp/scraper
          cd /tmp/scraper
          poetry config virtualenvs.create false
          poetry install --without dev
          pip install aiohttp asyncio

      - name: Maak output map aan
        run: mkdir -p data/raw data/csv

      - name: Stap 0 - Bestaande CSV data inladen (accumulate)
        run: |
          python3 << 'PYEOF'
          import gzip, shutil
          from pathlib import Path

          CSV = Path('data/csv')
          RAW = Path('data/raw')

          # Als er al een appearances.csv.gz is, decomprimeer die naar raw
          gz = CSV / 'appearances.csv.gz'
          if gz.exists():
              print(f"Bestaande appearances.csv.gz gevonden ({gz.stat().st_size // 1024} KB)")
              with gzip.open(gz, 'rb') as f_in:
                  with open(CSV / 'appearances_existing.csv', 'wb') as f_out:
                      shutil.copyfileobj(f_in, f_out)
              print("  → Gedecomprimeerd naar appearances_existing.csv")
          else:
              print("Geen bestaande appearances.csv.gz gevonden — eerste run")
          PYEOF

      - name: Stap 1 - Clubs scrapen
        run: |
          cd /tmp/scraper
          for SEASON in 2024 2025; do
            echo "=== Clubs seizoen $SEASON ==="
            python -m tfmkt clubs \
              -p $GITHUB_WORKSPACE/data/competitions.json \
              -s $SEASON \
              >> $GITHUB_WORKSPACE/data/raw/clubs.json || true
          done
          echo "Clubs nieuw: $(wc -l < $GITHUB_WORKSPACE/data/raw/clubs.json)"

      - name: Stap 2 - Spelers scrapen
        run: |
          cd /tmp/scraper
          for SEASON in 2024 2025; do
            echo "=== Spelers seizoen $SEASON ==="
            python -m tfmkt players \
              -p $GITHUB_WORKSPACE/data/raw/clubs.json \
              -s $SEASON \
              >> $GITHUB_WORKSPACE/data/raw/players.json || true
          done
          echo "Spelers nieuw: $(wc -l < $GITHUB_WORKSPACE/data/raw/players.json)"

      - name: Stap 2b - Marktwaarden ophalen
        timeout-minutes: 30
        continue-on-error: true
        run: |
          python3 << 'PYEOF'
          import json, asyncio, aiohttp
          from pathlib import Path

          RAW = Path('data/raw')
          PLAYERS_FILE = RAW / 'players.json'
          MV_FILE = RAW / 'market_values.json'

          MARKET_VALUES_API = "https://www.transfermarkt.com/ceapi/marketValueDevelopment/graph/"
          HEADERS = {
              "User-Agent": "transfermarkt-datasets/1.0 (https://github.com/arnauddebrabant2/transfermarkt-datasets)",
              "Accept": "application/json",
          }

          def get_player_ids():
              ids = []
              if not PLAYERS_FILE.exists():
                  return ids
              with open(PLAYERS_FILE, encoding='utf-8') as f:
                  for line in f:
                      line = line.strip()
                      if not line:
                          continue
                      try:
                          p = json.loads(line)
                          href = p.get('href', '')
                          parts = href.rstrip('/').split('/')
                          if 'spieler' in parts:
                              pid = parts[parts.index('spieler') + 1]
                              if pid:
                                  ids.append(pid)
                      except:
                          pass
              return ids

          async def fetch_mv(session, player_id, semaphore):
              async with semaphore:
                  url = f"{MARKET_VALUES_API}{player_id}"
                  try:
                      async with session.get(url, headers=HEADERS, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                          if resp.status == 200:
                              data = await resp.json(content_type=None)
                              values = data.get('list', [])
                              if values:
                                  latest = values[-1]
                                  mv = latest.get('mw', '')
                                  return player_id, mv
                  except:
                      pass
                  return player_id, None

          async def main():
              player_ids = get_player_ids()
              print(f"Marktwaarden ophalen voor {len(player_ids)} spelers...")
              semaphore = asyncio.Semaphore(10)
              results = {}
              async with aiohttp.ClientSession() as session:
                  tasks = [fetch_mv(session, pid, semaphore) for pid in player_ids]
                  done = 0
                  for coro in asyncio.as_completed(tasks):
                      pid, mv = await coro
                      if mv:
                          results[pid] = mv
                      done += 1
                      if done % 500 == 0:
                          print(f"  {done}/{len(player_ids)} verwerkt, {len(results)} met waarde")
              print(f"Klaar: {len(results)} marktwaarden gevonden")
              with open(MV_FILE, 'w', encoding='utf-8') as f:
                  json.dump(results, f)

          asyncio.run(main())
          PYEOF
          echo "Marktwaarden: $(wc -c < $GITHUB_WORKSPACE/data/raw/market_values.json 2>/dev/null || echo 0) bytes"

      - name: Stap 2c - Transferhistorie scrapen
        timeout-minutes: 30
        continue-on-error: true
        run: |
          cd /tmp/scraper
          for SEASON in 2024 2025; do
            echo "=== Transfers seizoen $SEASON ==="
            python -m tfmkt transfers \
              -p $GITHUB_WORKSPACE/data/raw/players.json \
              -s $SEASON \
              >> $GITHUB_WORKSPACE/data/raw/transfers.json || true
          done
          echo "Transfers: $(wc -l < $GITHUB_WORKSPACE/data/raw/transfers.json 2>/dev/null || echo 0)"

      - name: Stap 2d - Historische marktwaarden scrapen
        timeout-minutes: 30
        continue-on-error: true
        run: |
          cd /tmp/scraper
          for SEASON in 2024 2025; do
            echo "=== Valuations seizoen $SEASON ==="
            python -m tfmkt valuations \
              -p $GITHUB_WORKSPACE/data/raw/players.json \
              -s $SEASON \
              >> $GITHUB_WORKSPACE/data/raw/valuations.json || true
          done
          echo "Valuations: $(wc -l < $GITHUB_WORKSPACE/data/raw/valuations.json 2>/dev/null || echo 0)"


      - name: Stap 3 - Optredens scrapen
        timeout-minutes: 120
        continue-on-error: true
        run: |
          cd /tmp/scraper
          for SEASON in 2024 2025; do
            echo "=== Optredens seizoen $SEASON ==="
            python -m tfmkt appearances \
              -p $GITHUB_WORKSPACE/data/raw/players.json \
              -s $SEASON \
              >> $GITHUB_WORKSPACE/data/raw/appearances.json || true
          done
          echo "Optredens nieuw: $(wc -l < $GITHUB_WORKSPACE/data/raw/appearances.json)"

      - name: Stap 4 - Wedstrijden scrapen
        timeout-minutes: 60
        run: |
          cd /tmp/scraper
          for SEASON in 2024 2025; do
            echo "=== Wedstrijden seizoen $SEASON ==="
            python -m tfmkt games \
              -p $GITHUB_WORKSPACE/data/competitions.json \
              -s $SEASON \
              >> $GITHUB_WORKSPACE/data/raw/games.json || true
          done
          echo "Wedstrijden nieuw: $(wc -l < $GITHUB_WORKSPACE/data/raw/games.json)"

      - name: Stap 5 - JSON naar CSV omzetten en samenvoegen met bestaande data
        run: |
          python3 << 'PYEOF'
          import json, csv
          from pathlib import Path

          RAW = Path('data/raw')
          OUT = Path('data/csv')
          OUT.mkdir(exist_ok=True)

          def load_jsonl(path):
              items = []
              if not path.exists():
                  return items
              with open(path, encoding='utf-8') as f:
                  for line in f:
                      line = line.strip()
                      if line:
                          try: items.append(json.loads(line))
                          except: pass
              return items

          def load_csv_existing(path):
              rows = []
              if not path.exists():
                  return rows
              with open(path, encoding='utf-8') as f:
                  reader = csv.DictReader(f)
                  for row in reader:
                      rows.append(row)
              return rows

          def extract_id(href):
              if not href:
                  return ''
              parts = href.rstrip('/').split('/')
              if 'wettbewerb' in parts:
                  return parts[parts.index('wettbewerb') + 1]
              if 'verein' in parts:
                  return parts[parts.index('verein') + 1]
              if 'spieler' in parts:
                  return parts[parts.index('spieler') + 1]
              if 'spielbericht' in parts:
                  return parts[parts.index('spielbericht') + 1]
              return parts[-1]

          def parse_market_value(mv_str):
              if not mv_str:
                  return ''
              mv_str = str(mv_str).strip()
              try:
                  multiplier = 1
                  if 'm' in mv_str.lower():
                      multiplier = 1_000_000
                      mv_str = mv_str.lower().replace('m', '').replace('€', '').replace('£', '').strip()
                  elif 'k' in mv_str.lower() or 'th.' in mv_str.lower():
                      multiplier = 1_000
                      mv_str = mv_str.lower().replace('k', '').replace('th.', '').replace('€', '').replace('£', '').strip()
                  else:
                      mv_str = mv_str.replace('€', '').replace('£', '').replace(',', '').strip()
                  return int(float(mv_str) * multiplier)
              except:
                  return ''

          # Laad marktwaarden
          mv_lookup = {}
          mv_file = RAW / 'market_values.json'
          if mv_file.exists():
              with open(mv_file, encoding='utf-8') as f:
                  mv_raw = json.load(f)
                  for pid, mv in mv_raw.items():
                      mv_lookup[str(pid)] = parse_market_value(mv)
              print(f"Marktwaarden geladen: {len(mv_lookup)}")

          # ── CLUBS ──
          clubs = load_jsonl(RAW / 'clubs.json')
          club_lookup = {}
          new_club_rows = []
          for c in clubs:
              comp_href = c.get('parent', {}).get('href', '')
              comp_id = extract_id(comp_href)
              club_id = c.get('id') or extract_id(c.get('href', ''))
              club_lookup[c.get('href', '')] = {'club_id': club_id, 'name': c.get('name', ''), 'competition_id': comp_id}
              new_club_rows.append({
                  'club_id': club_id, 'name': c.get('name', ''),
                  'domestic_competition_id': comp_id,
                  'squad_size': c.get('squad_size', ''),
                  'average_age': c.get('average_age', ''),
                  'total_market_value': c.get('total_market_value', ''),
                  'coach_name': c.get('coach_name', ''),
                  'url': f"https://www.transfermarkt.co.uk{c.get('href', '')}",
              })

          # Samenvoegen met bestaande clubs (op club_id)
          existing_clubs = load_csv_existing(OUT / 'clubs.csv')
          existing_club_ids = {r['club_id'] for r in existing_clubs}
          merged_clubs = existing_clubs + [r for r in new_club_rows if str(r['club_id']) not in existing_club_ids]
          # Update bestaande met nieuwe data (nieuwste wint)
          club_id_map = {r['club_id']: r for r in existing_clubs}
          for r in new_club_rows:
              club_id_map[str(r['club_id'])] = r
          merged_clubs = list(club_id_map.values())

          if merged_clubs:
              with open(OUT / 'clubs.csv', 'w', newline='', encoding='utf-8') as f:
                  w = csv.DictWriter(f, fieldnames=list(merged_clubs[0].keys()))
                  w.writeheader(); w.writerows(merged_clubs)
              print(f'clubs.csv: {len(merged_clubs)} rijen (was {len(existing_clubs)}, +{len(merged_clubs)-len(existing_clubs)} nieuw)')

          # ── SPELERS ──
          players = load_jsonl(RAW / 'players.json')
          new_player_rows = []
          for p in players:
              club_href = p.get('parent', {}).get('href', '')
              club_info = club_lookup.get(club_href, {})
              player_id = p.get('id') or extract_id(p.get('href', ''))

              height_raw = p.get('height', '') or ''
              try:
                  height_cm = int(float(height_raw.replace('m','').replace(',','.').strip()) * 100)
              except:
                  height_cm = ''

              pos = p.get('position', '') or ''
              pos_map = {
                  'goalkeeper': 'Goalkeeper',
                  'centre-back': 'Defender', 'left-back': 'Defender', 'right-back': 'Defender', 'defender': 'Defender',
                  'attacking midfield': 'Midfield', 'midfielder': 'Midfield', 'midfield': 'Midfield',
                  'centre-forward': 'Attack', 'left winger': 'Attack', 'right winger': 'Attack', 'forward': 'Attack', 'attack': 'Attack'
              }
              position = next((v for k, v in pos_map.items() if k in pos.lower()), 'Midfield')

              mv = mv_lookup.get(str(player_id), '')
              if not mv:
                  mv_raw = p.get('current_market_value') or ''
                  try:
                      mv = int(str(mv_raw).replace('€','').replace(',','').strip()) if mv_raw else ''
                  except:
                      mv = ''

              mv_high_raw = p.get('highest_market_value') or ''
              try:
                  mv_high = int(str(mv_high_raw).replace('€','').replace(',','').strip()) if mv_high_raw else ''
              except:
                  mv_high = ''

              first_name = p.get('name', '') or ''
              last_name = p.get('last_name', '') or ''
              full_name = f"{first_name} {last_name}".strip() or first_name

              new_player_rows.append({
                  'player_id': player_id,
                  'name': full_name,
                  'first_name': first_name,
                  'last_name': last_name,
                  'date_of_birth': p.get('date_of_birth', ''),
                  'age': p.get('age', ''),
                  'country_of_citizenship': p.get('citizenship', ''),
                  'country_of_birth': (p.get('place_of_birth') or {}).get('country', ''),
                  'position': position,
                  'sub_position': pos,
                  'foot': p.get('foot', ''),
                  'height_in_cm': height_cm,
                  'market_value_in_eur': mv,
                  'highest_market_value_in_eur': mv_high,
                  'contract_expiration_date': p.get('contract_expires', ''),
                  'current_club_id': club_info.get('club_id', ''),
                  'current_club_name': club_info.get('name', ''),
                  'current_club_domestic_competition_id': club_info.get('competition_id', ''),
                  'image_url': p.get('image_url', ''),
                  'url': f"https://www.transfermarkt.co.uk{p.get('href', '')}",
              })

          # Samenvoegen met bestaande spelers (nieuwste wint op player_id)
          existing_players = load_csv_existing(OUT / 'players.csv')
          player_id_map = {r['player_id']: r for r in existing_players}
          for r in new_player_rows:
              player_id_map[str(r['player_id'])] = r
          merged_players = list(player_id_map.values())

          if merged_players:
              with open(OUT / 'players.csv', 'w', newline='', encoding='utf-8') as f:
                  w = csv.DictWriter(f, fieldnames=list(merged_players[0].keys()))
                  w.writeheader(); w.writerows(merged_players)
              print(f'players.csv: {len(merged_players)} rijen (was {len(existing_players)}, +{len(merged_players)-len(existing_players)} nieuw)')

          # ── OPTREDENS ──
          appearances = load_jsonl(RAW / 'appearances.json')
          new_app_rows = []
          for a in appearances:
              parent = a.get('parent') or {}
              player_href = parent.get('href', '')
              player_id = extract_id(player_href)
              club_href = (parent.get('current_club') or {}).get('href', '')
              player_club_id = extract_id(club_href)
              result_raw = a.get('result')
              if isinstance(result_raw, dict):
                  result_href = result_raw.get('href', '')
              elif isinstance(result_raw, str):
                  result_href = result_raw
              else:
                  result_href = ''
              game_id = extract_id(result_href)
              date = a.get('date', '')
              appearance_id = f"{player_id}_{date.replace('/', '')}" if player_id and date else ''

              min_raw = a.get('minutes_played', '') or ''
              try:
                  minutes_played = int(str(min_raw).replace("'", "").strip())
              except:
                  minutes_played = 0

              def card_count(val):
                  if val is None or val == '' or val == 0: return 0
                  return 1
              def safe_int(val):
                  try: return int(val) if val != '' and val is not None else 0
                  except: return 0

              new_app_rows.append({
                  'appearance_id': appearance_id,
                  'game_id': game_id,
                  'player_id': player_id,
                  'player_club_id': player_club_id,
                  'date': date,
                  'player_name': f"{parent.get('name', '')} {parent.get('last_name', '')}".strip(),
                  'competition_id': a.get('competition_code', ''),
                  'yellow_cards': card_count(a.get('yellow_cards')),
                  'red_cards': card_count(a.get('red_cards')),
                  'goals': safe_int(a.get('goals')),
                  'assists': safe_int(a.get('assists')),
                  'minutes_played': minutes_played,
              })

          # Samenvoegen met bestaande appearances (op appearance_id)
          existing_apps = load_csv_existing(OUT / 'appearances_existing.csv')
          app_id_map = {r['appearance_id']: r for r in existing_apps}
          for r in new_app_rows:
              if r['appearance_id']:
                  app_id_map[r['appearance_id']] = r
          merged_apps = list(app_id_map.values())

          if merged_apps:
              with open(OUT / 'appearances.csv', 'w', newline='', encoding='utf-8') as f:
                  w = csv.DictWriter(f, fieldnames=list(merged_apps[0].keys()))
                  w.writeheader(); w.writerows(merged_apps)
              print(f'appearances.csv: {len(merged_apps)} rijen (was {len(existing_apps)}, +{len(merged_apps)-len(existing_apps)} nieuw)')

          # ── WEDSTRIJDEN ──
          games = load_jsonl(RAW / 'games.json')
          new_game_rows = []
          for g in games:
              result = g.get('result', '') or ''
              try:
                  home_goals, away_goals = result.split(':')
                  home_goals = home_goals.strip()
                  away_goals = away_goals.strip()
              except:
                  home_goals, away_goals = '', ''

              home_href = (g.get('home_club') or {}).get('href', '')
              away_href = (g.get('away_club') or {}).get('href', '')
              home_id = extract_id(home_href)
              away_id = extract_id(away_href)
              comp_id = (g.get('parent') or {}).get('country_code', '') or \
                        extract_id((g.get('parent') or {}).get('href', ''))

              new_game_rows.append({
                  'game_id': g.get('game_id', ''),
                  'competition_id': comp_id,
                  'season': g.get('season', '2025'),
                  'date': g.get('date', ''),
                  'home_club_id': home_id,
                  'away_club_id': away_id,
                  'home_club_goals': home_goals,
                  'away_club_goals': away_goals,
                  'home_club_name': g.get('home_club_name', ''),
                  'away_club_name': g.get('away_club_name', ''),
                  'stadium': g.get('stadium', ''),
                  'attendance': g.get('attendance', ''),
                  'url': f"https://www.transfermarkt.co.uk{g.get('href', '')}",
              })

          # Samenvoegen met bestaande games (op game_id)
          existing_games = load_csv_existing(OUT / 'games.csv')
          game_id_map = {r['game_id']: r for r in existing_games}
          for r in new_game_rows:
              if r['game_id']:
                  game_id_map[str(r['game_id'])] = r
          merged_games = list(game_id_map.values())

          if merged_games:
              with open(OUT / 'games.csv', 'w', newline='', encoding='utf-8') as f:
                  w = csv.DictWriter(f, fieldnames=list(merged_games[0].keys()))
                  w.writeheader(); w.writerows(merged_games)
              print(f'games.csv: {len(merged_games)} rijen (was {len(existing_games)}, +{len(merged_games)-len(existing_games)} nieuw)')

          # ── TRANSFERS ──
          transfers = load_jsonl(RAW / 'transfers.json')
          new_transfer_rows = []
          for t in transfers:
              parent = t.get('parent') or {}
              player_href = parent.get('href', '')
              player_id = extract_id(player_href)
              from_href = (t.get('from_club') or {}).get('href', '')
              to_href   = (t.get('to_club')   or {}).get('href', '')
              new_transfer_rows.append({
                  'player_id':       player_id,
                  'transfer_date':   t.get('date', ''),
                  'transfer_season': t.get('season', ''),
                  'from_club_id':    extract_id(from_href),
                  'from_club_name':  (t.get('from_club') or {}).get('name', ''),
                  'to_club_id':      extract_id(to_href),
                  'to_club_name':    (t.get('to_club') or {}).get('name', ''),
                  'transfer_fee':    t.get('fee', ''),
                  'market_value_at_transfer': t.get('market_value', ''),
              })
          existing_transfers = load_csv_existing(OUT / 'transfers.csv')
          transfer_key = lambda r: f"{r.get('player_id','')}_{r.get('transfer_date','')}_{r.get('to_club_id','')}"
          transfer_map = {transfer_key(r): r for r in existing_transfers}
          for r in new_transfer_rows:
              transfer_map[transfer_key(r)] = r
          merged_transfers = list(transfer_map.values())
          if merged_transfers:
              with open(OUT / 'transfers.csv', 'w', newline='', encoding='utf-8') as f:
                  w = csv.DictWriter(f, fieldnames=list(merged_transfers[0].keys()))
                  w.writeheader(); w.writerows(merged_transfers)
              print(f'transfers.csv: {len(merged_transfers)} rijen (was {len(existing_transfers)}, +{len(merged_transfers)-len(existing_transfers)} nieuw)')

          # ── VALUATIONS ──
          valuations = load_jsonl(RAW / 'valuations.json')
          new_val_rows = []
          for v in valuations:
              parent = v.get('parent') or {}
              player_href = parent.get('href', '')
              player_id = extract_id(player_href)
              new_val_rows.append({
                  'player_id':             player_id,
                  'date':                  v.get('date', ''),
                  'market_value_in_eur':   parse_market_value(v.get('market_value', '')),
                  'current_club_id':       extract_id((v.get('current_club') or {}).get('href', '')),
                  'player_club_domestic_competition_id': v.get('competition_code', ''),
              })
          existing_vals = load_csv_existing(OUT / 'player_valuations.csv')
          val_key = lambda r: f"{r.get('player_id','')}_{r.get('date','')}"
          val_map = {val_key(r): r for r in existing_vals}
          for r in new_val_rows:
              val_map[val_key(r)] = r
          merged_vals = list(val_map.values())
          if merged_vals:
              with open(OUT / 'player_valuations.csv', 'w', newline='', encoding='utf-8') as f:
                  w = csv.DictWriter(f, fieldnames=list(merged_vals[0].keys()))
                  w.writeheader(); w.writerows(merged_vals)
              print(f'player_valuations.csv: {len(merged_vals)} rijen (was {len(existing_vals)}, +{len(merged_vals)-len(existing_vals)} nieuw)')

          # Ruim tijdelijk bestand op
          tmp = Path('data/csv/appearances_existing.csv')
          if tmp.exists():
              tmp.unlink()

          print('Conversie en samenvoegen klaar!')
          PYEOF

      - name: Commit en push CSV's
        run: |
          git config user.name "ScoutAI Bot"
          git config user.email "actions@users.noreply.github.com"
          gzip -f data/csv/appearances.csv
          git add data/csv/ data/csv/transfers.csv data/csv/player_valuations.csv
          TIMESTAMP=$(date -u '+%Y-%m-%d %H:%M UTC')
          BATCH=$(basename $(cat $GITHUB_WORKSPACE/data/competitions.json | python3 -c "import sys,json; lines=[json.loads(l) for l in sys.stdin if l.strip()]; codes=[l.get('country_code','') for l in lines[:3]]; print('+'.join(codes))" 2>/dev/null) 2>/dev/null || echo "batch")
          git commit -m "ScoutAI data update: ${TIMESTAMP} [${BATCH}...]" || echo "Geen wijzigingen"
          git push

      - name: Samenvatting
        run: |
          echo "=== SCRAPING KLAAR ==="
          for f in data/csv/*; do
            echo "$f: $(wc -c < $f) bytes"
          done
